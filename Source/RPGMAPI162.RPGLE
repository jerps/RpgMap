**FREE
	
if %parms >  1;
  k( 1) = rm_x(k01);
endif;
if %parms >  2;
  k( 2) = rm_x(k02);
endif;
if %parms >  3;
  k( 3) = rm_x(k03);
endif;
if %parms >  4;
  k( 4) = rm_x(k04);
endif;
if %parms >  5;
  k( 5) = rm_x(k05);
endif;
if %parms >  6;
  k( 6) = rm_x(k06);
endif;
if %parms >  7;
  k( 7) = rm_x(k07);
endif;
if %parms >  8;
  k( 8) = rm_x(k08);
endif;
if %parms >  9;
  k( 9) = rm_x(k09);
endif;
if %parms > 10;
  k(10) = rm_x(k10);
endif;
