**FREE
	
if %parms >  1;
  k( 1) = rm_sa(k01);
endif;
if %parms >  2;
  k( 2) = rm_sa(k02);
endif;
if %parms >  3;
  k( 3) = rm_sa(k03);
endif;
if %parms >  4;
  k( 4) = rm_sa(k04);
endif;
if %parms >  5;
  k( 5) = rm_sa(k05);
endif;
if %parms >  6;
  k( 6) = rm_sa(k06);
endif;
if %parms >  7;
  k( 7) = rm_sa(k07);
endif;
if %parms >  8;
  k( 8) = rm_sa(k08);
endif;
if %parms >  9;
  k( 9) = rm_sa(k09);
endif;
if %parms > 10;
  k(10) = rm_sa(k10);
endif;
